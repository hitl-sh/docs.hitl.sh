---
title: "API Keys"
description: "Generate and manage API keys to securely integrate HITL.sh with your applications and workflows"
---

# API Keys

API keys are your secure credentials for integrating HITL.sh with your applications, workflows, and third-party tools. They authenticate your requests and ensure only authorized systems can interact with your loops.

## Understanding API Keys

API keys in HITL.sh provide secure access to:

- **Loop Management** — Create, read, update, and delete loops
- **Request Operations** — Submit requests and retrieve responses
- **Loop Members** — Add and remove reviewers from loops
- **Webhook Configuration** — Set up event notifications
- **Request Feedback** — Add feedback to completed requests

<Warning>
Keep your API keys secure and never expose them in client-side code or public repositories. Treat them like passwords.
</Warning>

## Your API Key

Navigate to **API Keys** in the sidebar at [my.hitl.sh](https://my.hitl.sh). Your API key and history are displayed in a single card:

### Current Key

Your active API key is shown in a rounded input field with two actions:

- **Reveal** (eye icon) — Show the full key (hidden by default)
- **Copy** (copy icon) — Copy the full key to your clipboard

### Regenerating Your Key

Click **Regenerate** to create a new key. This will:

1. Deactivate your current key immediately
2. Generate a new key
3. Show the new key for you to copy

<Warning>
Regenerating your key will break any integrations using the old key. Update your environment variables immediately after regenerating.
</Warning>

### Key History

Below the current key, you'll see a history table showing all your API keys — active and deactivated — with their status, creation date, and last used timestamp.

## Using Your API Key

### Authentication Header

Include your API key in the `Authorization` header of all API requests:

```bash
Authorization: Bearer YOUR_API_KEY
```

### Testing Your API Key

Verify your key is working with the test endpoint:

<CodeGroup>
```bash cURL
curl -X GET 'https://api.hitl.sh/v1/test' \
  -H 'Authorization: Bearer YOUR_API_KEY'
```

```python Python
import requests

response = requests.get(
    'https://api.hitl.sh/v1/test',
    headers={'Authorization': 'Bearer YOUR_API_KEY'}
)
print(response.json())
```

```javascript Node.js
const response = await fetch('https://api.hitl.sh/v1/test', {
  headers: { 'Authorization': 'Bearer YOUR_API_KEY' }
});
const data = await response.json();
console.log(data);
```
</CodeGroup>

## Rate Limits

API keys have rate limits to ensure fair usage:

- **100 requests per hour** per API key
- Rate limits reset at the top of each hour
- Rate limit info is included in response headers

**Rate Limit Headers:**
```
X-RateLimit-Limit: 100
X-RateLimit-Remaining: 85
X-RateLimit-Reset: 1642237200
```

If you hit the rate limit, you'll receive a `429` status code. Wait for the reset or implement exponential backoff.

## Security Best Practices

<CardGroup cols={2}>
<Card title="Environment Variables" icon="lock">
Store API keys in environment variables, never hardcode them in source code.
</Card>

<Card title="Key Rotation" icon="arrows-rotate">
Regenerate your key periodically to minimize the impact of potential leaks.
</Card>
</CardGroup>

## Troubleshooting

| Error | Cause | Fix |
|-------|-------|-----|
| `401 Unauthorized` | Invalid or missing API key | Check the key is correct and uses `Bearer` prefix |
| `403 Forbidden` | Key lacks required permissions | Verify your account plan supports the feature |
| `429 Rate Limited` | Too many requests | Wait for rate limit reset, implement backoff |

## Next Steps

<CardGroup cols={2}>
<Card title="Create Your First Loop" icon="plus" href="/first-loop">
Build your first human-in-the-loop workflow.
</Card>

<Card title="Billing & Plans" icon="credit-card" href="/billing">
Understand usage-based pricing.
</Card>
</CardGroup>
