---
title: "n8n Integration"
description: "Trigger HITL.sh workflows from n8n using webhook URLs for human-in-the-loop automation"
---

# n8n Integration

Integrate HITL.sh with n8n to add human decision-making to your automated workflows. The HITL.sh plugin for n8n sends data to your workflow's webhook URL and waits for the human review response.

<Frame>
<img src="/images/integrations/hitl-pics/n8n-workflow.png" alt="n8n workflow with HITL.sh integration" />
</Frame>

## Why Use n8n with HITL.sh?

<CardGroup cols={2}>
<Card title="Visual Workflow Builder" icon="diagram-project">
Design complex automation workflows with an intuitive drag-and-drop interface
</Card>

<Card title="300+ Integrations" icon="plug">
Connect HITL.sh with popular services like Slack, Gmail, databases, and more
</Card>

<Card title="Self-Hosted Option" icon="server">
Deploy on your own infrastructure for complete data control and privacy
</Card>

<Card title="Open Source" icon="code">
Fully transparent codebase you can customize and extend to your needs
</Card>
</CardGroup>

## Installation Methods

n8n offers two ways to add HITL.sh integration to your workflows:

<Tabs>
<Tab title="Community Nodes (Recommended)">
### Install via Community Nodes

<Steps>
<Step title="Open n8n Settings">
Navigate to **Settings** -> **Community Nodes** in your n8n instance
</Step>

<Step title="Install HITL.sh Node">
Search for `@hitlsh/n8n-nodes-hitl` and click **Install**

<Frame>
<img src="/images/integrations/hitl-pics/n8n-community-nodes-installation.png" alt="Installing HITL.sh community node in n8n" />
</Frame>
</Step>

<Step title="Restart n8n">
Restart your n8n instance to load the new nodes
</Step>

<Step title="Start Building">
The HITL.sh nodes will now appear in your node palette
</Step>
</Steps>

<Note>
Community Nodes are the recommended installation method as they receive automatic updates and simplified configuration.
</Note>
</Tab>

<Tab title="Self-Hosted NPM">
### Install via NPM

For self-hosted n8n instances with custom node installations:

```bash
cd ~/.n8n/nodes
npm install @hitlsh/n8n-nodes-hitl
n8n restart
```

<Warning>
Self-hosted NPM installation requires manual updates when new versions are released.
</Warning>
</Tab>
</Tabs>

## Setting Up Credentials

<Steps>
<Step title="Get Your API Key">
1. Log in to your HITL.sh dashboard at [my.hitl.sh](https://my.hitl.sh)
2. Navigate to **Settings** -> **API Keys**
3. Click **Generate New API Key**
4. Copy your API key

<Frame>
<img src="/images/integrations/hitl-pics/n8n-api-credentials-setup.png" alt="Setting up HITL.sh API credentials in n8n" />
</Frame>
</Step>

<Step title="Add Credentials in n8n">
1. In n8n, go to **Credentials** -> **New**
2. Search for "HITL.sh"
3. Paste your API key
</Step>

<Step title="Test Connection">
Click **Test Credentials** to verify the connection
</Step>
</Steps>

## How It Works

The HITL.sh n8n plugin triggers a workflow by sending a JSON payload to the workflow's webhook URL. The execution pauses at the human review node, and the plugin polls for the response.

<Steps>
<Step title="Get your workflow webhook URL">
In the HITL.sh dashboard, open your workflow and copy the webhook URL from the Settings tab.
</Step>

<Step title="Configure the n8n node">
Paste the webhook URL into the HITL.sh node configuration in n8n.
</Step>

<Step title="Map your data">
Map fields from your n8n workflow into the JSON payload that will be sent to the HITL.sh webhook.
</Step>

<Step title="n8n sends the payload">
When the n8n workflow runs, it sends the JSON payload to the webhook URL, triggering a HITL.sh workflow execution.
</Step>

<Step title="Human reviews and responds">
The HITL.sh workflow pauses at the human review node. A reviewer responds via the mobile app or web dashboard.
</Step>

<Step title="n8n receives the response">
The plugin polls for the response and returns it to your n8n workflow for downstream processing.
</Step>
</Steps>

## Node Configuration

<Frame>
<img src="/images/integrations/hitl-pics/n8n-node-configuration.png" alt="Configuring HITL.sh node parameters in n8n" />
</Frame>

### Required Parameters

<ParamField body="Webhook URL" type="string" required>
The webhook URL of your HITL.sh workflow (found in the workflow's Settings tab)
</ParamField>

<ParamField body="Payload" type="object" required>
The JSON data to send to the workflow trigger. This data is available to all nodes in the HITL.sh workflow.
</ParamField>

### Optional Parameters

<ParamField body="API Key" type="string">
Your HITL.sh API key for authenticated webhook requests
</ParamField>

<ParamField body="Wait for Response" type="boolean" default="true">
Whether to wait for the human review response before continuing the n8n workflow
</ParamField>

## Response Data Structure

When a reviewer completes a response, the n8n node receives:

<Frame>
<img src="/images/integrations/hitl-pics/n8n-response-data-structure.png" alt="Complete response data structure from HITL.sh in n8n" />
</Frame>

```json
{
  "execution_id": "exec_abc123",
  "status": "completed",
  "response_data": "approve",
  "response_by_user": {
    "id": "65f1234567890abcdef12350",
    "name": "John Reviewer",
    "email": "john@example.com"
  },
  "response_time_seconds": 145.5,
  "created_at": "2024-03-15T10:30:00Z",
  "completed_at": "2024-03-15T10:32:25Z"
}
```

## Example Workflow: Customer Refund Approval

<Frame>
<img src="/images/integrations/hitl-pics/customer-refund-approval-workflow.png" alt="n8n workflow for customer refund approvals with HITL.sh" />
</Frame>

### Workflow Steps

<Steps>
<Step title="Trigger: New Refund Request">
Webhook or form submission triggers when customer requests refund
</Step>

<Step title="Check Refund Amount">
Conditional node routes based on refund amount:
- Under $50: Auto-approve
- $50+: Send to HITL.sh for human review
</Step>

<Step title="HITL.sh Node">
Send the refund details to your HITL.sh workflow webhook URL:

```json
{
  "content": "Customer requesting ${{ $json.amount }} refund\nReason: {{ $json.reason }}\nCustomer lifetime value: ${{ $json.customer_ltv }}",
  "customer_id": "{{ $json.customer_id }}",
  "order_id": "{{ $json.order_id }}",
  "amount": "{{ $json.amount }}"
}
```

The HITL.sh workflow handles response type, options, reviewers, and timeout -- all configured in the workflow designer.
</Step>

<Step title="Process Decision">
Switch node routes based on human decision
</Step>

<Step title="Execute Action">
- **Approved**: Process refund and send confirmation
- **Denied**: Send explanation email
- **Escalate**: Notify management team
</Step>
</Steps>

## Best Practices

<CardGroup cols={2}>
<Card title="Clear Payload Data" icon="message">
Include all relevant context in your webhook payload so the HITL.sh workflow can display it to reviewers
</Card>

<Card title="Handle Timeouts" icon="clock">
Configure your n8n workflow to handle cases where the HITL.sh response times out
</Card>

<Card title="Safe Defaults" icon="shield">
Set conservative default responses in your HITL.sh workflow for timeout scenarios
</Card>

<Card title="Error Handling" icon="alert-triangle">
Add error nodes in n8n to handle webhook failures gracefully
</Card>
</CardGroup>

## Troubleshooting

<AccordionGroup>
<Accordion title="Node Not Appearing">
**Solution**:
- Verify installation completed successfully
- Restart n8n instance
- Check n8n logs for installation errors
</Accordion>

<Accordion title="Authentication Failed">
**Solution**:
- Verify API key is correct
- Check API key has required permissions
- Ensure API key hasn't expired
</Accordion>

<Accordion title="No Response Received">
**Solution**:
- Verify the HITL.sh workflow is activated
- Check that the webhook URL is correct
- Ensure the workflow has human review nodes with assigned reviewers
- Check reviewer notification settings
</Accordion>
</AccordionGroup>

## Next Steps

<CardGroup cols={2}>
<Card title="Zapier Integration" icon="zap" href="/integrations/zapier">
Learn about our Zapier integration
</Card>

<Card title="Make Integration" icon="puzzle" href="/integrations/make">
Explore Make.com integration
</Card>

<Card title="Workflow Designer" icon="pen-ruler" href="/guides/workflow-designer">
Configure your HITL.sh workflows
</Card>

<Card title="Community Support" icon="discord" href="https://discord.gg/jYVrpmnMP9">
Join our Discord community for help and examples
</Card>
</CardGroup>
